services:
  app:
    build: .
    working_dir: /workspace
    volumes:
      - .:/workspace
      - hit-and-out-node-modules:/workspace/node_modules
    ports:
      - "5173:5173"

volumes:
  hit-and-out-node-modules:
